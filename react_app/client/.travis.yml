language: generic
sudo: required

services:
  - docker

before_install:
  - docker build -t iaredockerhugger/react_app  -f Dockerfile.dev .

script:
  - docker run -e CI=true USERNAME/react-app npm run test

